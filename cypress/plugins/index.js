// const rewriteUrl = require('a_common_internal_pkg').default;

// module.exports = (on, config) => {
//     config.baseUrl = rewriteUrl(process.env.WWW_COM_URL);
//     config.env.BASE_HOST = rewriteUrl(process.env.WWW_COM_URL);
//     return config;
// };
